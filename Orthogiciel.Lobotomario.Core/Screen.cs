using System;
using System.Collections.Generic;
using System.Drawing;
using System.Linq;
using System.Text;
using System.Threading.Tasks;

namespace Orthogiciel.Lobotomario.Core
{
    public class Screen
    {
        //public static bool FindSprite(Bitmap sprite, Bitmap image)
        //{
        //    for (var x = 0; x < image.Width; x++)
        //    {
        //        for (var y = 0; y < image.Height; y++)
        //        {
        //            var found = true;
        //            //var k = x;
        //            //var l = y;

        //            for (var x_sprite = 0; x_sprite < sprite.Width; x_sprite++)
        //            {
        //                //l = y;

        //                for (var y_sprite = 0; y_sprite < sprite.Height; y_sprite++)
        //                {
        //                    if (sprite.GetPixel(x_sprite, y_sprite) != image.GetPixel(x + x_sprite, y + y_sprite))
        //                    {
        //                        found = false;
        //                        break;
        //                    }
        //                }

        //                if (!found)
        //                    break;
        //            }

        //            if (found)
        //                return true;
        //        }
        //    }

        //    return false;
        //}
    }
}
